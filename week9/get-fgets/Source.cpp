

int main()
{






}// Example 1: Using gets() to read a string from user input
// Syntax: char* gets(char *str);

// Declare a character array to store user input
// Use gets() to take input from the user (Note: gets() is unsafe and deprecated)
// Print the inputted string










// Example 2: Using fgets() to safely read a string from user input
// Syntax: char* fgets(buffer, size, stdin);

// Declare a character array with a defined size
// Use fgets() to read input from stdin (standard input) with a size limit
// fgets() prevents buffer overflow by restricting input length
// Print the inputted string
