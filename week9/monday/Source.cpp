// Example 1: Using strcpy() to copy a string
#include<string.h>
int main()
{
	//use the strncpy
	//strncpy_s() 

	// Declare a source string with some text
// Declare a destination string with enough space to store the copied text
// Use strcpy() to copy the source string into the destination
// Print both strings to verify the copy operation

}












// Example 2: Using strcmp() and stricmp() to compare strings

// Declare two strings with different cases
// Use strcmp() to compare them (case-sensitive comparison)
// If the result is 0, the strings are identical; otherwise, they are different
// Use stricmp() (or strcasecmp() on Linux/macOS) to compare them (case-insensitive comparison)
// Again, check the result to determine if the strings are equal regardless of case
// Print appropriate messages based on the comparison results
